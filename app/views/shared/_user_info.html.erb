<%= link_to gravatar_for(current_user), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("shared._user_info.view"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>
